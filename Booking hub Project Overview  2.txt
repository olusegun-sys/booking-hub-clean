**Plain English summary:** Here's the full updated project overview — 22 September 2026 — carrying forward everything from the previous version, adding today's session (homepage real-businesses fix, backend `rooms` join, removal of the fake destination/popular-stays sections), and structured as a clean handoff document for the next chat.

I've kept the format you established, folded in today's work, and added a "For the Next Chat: Pick Up Here" section that reflects the state of the code **right now** — after this session but before you've run the local test.

---

# Plazzaa (formerly Booking Hub) — Complete Project Overview
## MASTER UPDATE — 22 SEPTEMBER 2026 (POST-DEMO-DAY, PRE-HOMEPAGE-FIX-SHIP)

## 📋 Plain English Summary

This document captures everything we've built — from the first line of code through the homepage data-integrity sprint on 22 September. It's the single source of truth for the Plazzaa for Business platform. If this chat hits the limit, the next chat picks up here. Read it fully before writing any code.

**What changed since 21 September:** The homepage had fake businesses (hardcoded location cards + hardcoded "popular stays" + hardcoded stock images keyed by business_type). A UX designer working with Olusegun spotted this on 22 September. We designed and implemented a fix that removes all fake content and replaces it with a real "Available on Plazzaa" grid driven by `GET /api/businesses` — pulled from Supabase, filtered to `status='approved' AND is_active=true`, showing the actual image the business owner uploaded in their dashboard. **The code is written. Local testing is the next step.**

---

## 🎯 Project Vision & Mission

### What We're Building

**Plazzaa** (formerly Booking Hub) is a multi-tenant booking management platform for Nigerian businesses. Think of it as **"Shopify for bookings"** — not a marketplace like Booking.com, but a platform where each business gets their own branded booking page, accepts payments (Paystack or pay-at-venue), receives automated email notifications, and tracks all bookings in a real-time dashboard.

**Part of the Plazzaa ecosystem:** The platform is the "Plazzaa for Business" solution — a comprehensive business management tool. The product was rebranded from **Booking Hub** to **Plazzaa** on 18–21 September 2026.

### Major Strategic Pivot: From Hospitality-Only to Any Business Type

As of the 14 September 2026 meeting, the platform serves three groups:

- **Stays** — Hotels, apartments, event halls
- **Food** — Restaurants, diners, cafés, catering, other food
- **Others** — Sports facilities, spas, beauty salons, nail studios, car washes, activity places

The dashboard, signup flow, landing page, emails, homepage, and booking page all adapt per business type via a **config-driven label system**.

### Key Meeting Decisions

**From 22 September 2026 meeting (Boss Terrence pivot):**
- ✅ Strategic shift toward B2B software focus (consumer marketplace temporarily deprioritised)
- ✅ Two-to-three-month free pilot setup for select businesses to build social proof
- ✅ Free trial period of 30–60 days on paid subscription tiers
- ✅ Target deployment: **Wednesday 24 September, worst-case Friday 26 September**
- ✅ Emmanuel assigned to merge UI changes into `main`
- ✅ Terrence to review platform once deployed and provide feedback
- ⚠️ Note: no test environment yet — updates deploy directly live (Yannick)

**From 14 September 2026 meeting:**
- ✅ Landing page prioritises businesses and hosts — *shipped*
- ✅ CTA change: "Start Free Trial" → **"Get Started"** — *shipped*
- ✅ Business categories restructured: **Stays / Food / Others** — *shipped*
- ✅ "Rooms" tab renamed to **"Menu & Services"** — *shipped*
- ⏳ Guest count moved from booking page → booking confirmation only — *pending (Jeremiah)*
- ⏳ Plazzaa business vetting standards — *deferred*

**From earlier September meeting:**
- Iterate, don't rebuild
- Independent B2B product
- Pilot with 5–10 businesses
- Per-booking fee model — initial monetisation
- Subscription tiers retained — Free / Starter (₦30k) / Pro (₦50k)
- Staff feature removed — files kept for future use

### The Problem We're Solving

Nigerian businesses struggle with:
- ❌ No online presence or booking system
- ❌ Manual booking management (paper, WhatsApp, phone calls)
- ❌ No way to accept online payments
- ❌ No analytics or business insights
- ❌ Expensive custom development

### Our Solution

- ✅ Free branded booking page per business
- ✅ Real-time booking management dashboard
- ✅ Paystack integration for online payments
- ✅ **Automated email notifications (WORKING end-to-end since 19 Sept)**
- ✅ Mobile-first design (Nigeria is mobile-first)
- ✅ Shareable booking links for WhatsApp/social
- ✅ Database-driven content management
- ✅ Multi-item support (services, menu items, rooms, courts, appointments)
- ✅ Per-item features, amenities, area guide, property details, images
- ✅ Professional booking receipt
- ✅ **Business-type-aware emails** ("Order Confirmed" for food, "Appointment Confirmed" for spa/salon, "Court Booked" for sports)
- ✅ **Business-type-aware booking pages**
- ✅ **Custom domain support** (`myplazzaa.com` live + per-business custom domains)
- ✅ **Broad business category support** (Stays / Food / Others) with group search
- ✅ **Plazzaa brand identity live** — logo, favicon, tab title
- ✅ **Responsive across all devices** — 375px mobile → 1280px+ desktop
- ✅ **Complete business-type config** — 15+ types supported in dashboard
- ✅ **Scoped auth tokens** — admin and business sessions no longer clobber each other
- ✅ **🆕 Homepage shows only real, approved businesses** — no more hardcoded fake cards

---

## 🏗️ Architecture Overview

### System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ Homepage │  │   Admin  │  │ Business │  │ Unified  │  │
│  │ (Stays/  │  │Dashboard │  │Dashboard │  │ Booking  │  │
│  │ Food/    │  │          │  │(config)  │  │Page      │  │
│  │ Others,  │  │          │  │          │  │          │  │
│  │ real     │  │          │  │          │  │          │  │
│  │ approved │  │          │  │          │  │          │  │
│  │ only 🆕) │  │          │  │          │  │          │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │  Owner   │  │ Business │  │  Menu &  │                 │
│  │Properties│  │ Settings │  │ Services │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│  ┌──────────┐  ┌──────────┐                                │
│  │  Host    │  │ Signup   │                                │
│  │ Landing  │  │ Flow     │                                │
│  └──────────┘  └──────────┘                                │
│  ┌────────────────────────────────────────────┐             │
│  │ Plazzaa logo, favicon, tab title — LIVE    │             │
│  └────────────────────────────────────────────┘             │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTPS/REST API
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    BACKEND (Node.js + Express)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │   Auth   │  │ Business │  │ Bookings │  │  Email   │  │
│  │          │  │   Mgmt   │  │          │  │ Service  │  │
│  │          │  │          │  │          │  │ (labels) │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ Enhanced │  │  Item    │  │  Domain  │                 │
│  │ Business │  │  Images  │  │   DNS    │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│  ┌──────────┐  ┌─────────────────────┐                     │
│  │  Search  │  │ /api/businesses 🆕  │                     │
│  │  (group) │  │ now joins rooms +   │                     │
│  │          │  │ filters is_active   │                     │
│  └──────────┘  └─────────────────────┘                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              SUPABASE (PostgreSQL + Storage)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Users   │  │Businesses│  │ Bookings │  │  Images  │  │
│  │          │  │(enhanced)│  │          │  │(Storage) │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│  ┌──────────┐                                               │
│  │  Rooms   │  ← features, amenities, area_guide, images   │
│  │ (items)  │  ← property_type, bathrooms, parking_spaces  │
│  └──────────┘  ← furnishing_status, listing_status         │
│                ← images (JSONB array per item)             │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ Paystack │  │  Resend  │  │   DNS    │                 │
│  │(Payments)│  │ (Email)  │  │Verification│               │
│  │ (test)   │  │(verified)│  │          │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

### Data Flow — Booking

1. Customer visits business booking page (`myplazzaa.com/book/{slug}` or custom domain)
2. Customer selects item/date → chooses payment method
3. Frontend sends booking request to backend API
4. Backend validates → creates booking in database → triggers email (business-type-aware)
5. If Paystack: redirects to payment → verifies on callback → confirms booking
6. If Pay at Venue: creates booking immediately → sends confirmation email
7. Business Owner sees booking in dashboard → manages status

### Data Flow — Business Search

1. User clicks a category card (**Stays / Food / Others**) on HomePage
2. Frontend sends comma-separated types for the group:
   - `stays` → `hotel,apartment,event_hall,event`
   - `food` → `restaurant,diner,cafe,other_food`
   - `others` → `sports,spa,beauty_salon,activity_place`
3. Backend splits into array, uses `.in('business_type', types)` when >1
4. Results returned and rendered with group-aware labels

### Data Flow — Homepage "Available on Plazzaa" 🆕

1. HomePage mounts → `useEffect` fires `GET /api/businesses`
2. Backend queries `businesses` with nested `rooms(id, images, name)` select, filtered to `status='approved'` AND `is_active=true`
3. Response contains each business + their rooms
4. Frontend `resolveBusinessImage()` picks the image in this priority:
   - `business.cover_image` (legacy)
   - `business.logo_url` (what BusinessProfile writes today)
   - `business.rooms[0].images[0]` (first item's primary image from RoomPage)
   - `null` → neutral "No image yet" placeholder
5. Card renders with real name, real area (`city, state`), real description (if set), correct business-type badge
6. Click → `navigate('/book/{slug}')` — no location check, no toast

### Data Flow — Business Signup

1. Business visits `myplazzaa.com` → clicks "Become a Host" or "Get Started"
2. **BusinessSignup.jsx** — 2-step flow: Account details → Location
3. Business type selected from grouped dropdown (Stays / Food / Others)
4. Backend creates `businesses` row with `status: 'pending'`
5. `sendWelcomeEmail` fires (uses `bookings@myplazzaa.com` sender)
6. Admin approves via dashboard → `sendApprovalEmail` fires with live booking link
7. Business now visible on homepage's "Available on Plazzaa" grid 🆕

### Data Flow — Auth (Scoped Tokens)

**Before 21 Sept:** Both admin and business logins wrote to the same `auth_token` localStorage key. Logging into a business overwrote the admin token; logging out of a business wiped both.

**After 21 Sept:** Scoped tokens.

| Session | Storage Key | Used By |
|---------|-------------|---------|
| Admin | `admin_token` | `/api/admin/*` routes |
| Business | `business_token` | All other API routes |
| Legacy fallback | `auth_token`, `token` | Read-only migration path |

### Business-Type-Aware System

The core architectural pattern used across the platform: **one config map, keyed by `business_type`, drives all visible labels.**

Five places this exists:
- **`client/src/HomePage.jsx`** — `categoryGroups`, `businessTypeDisplay` maps
- **`client/src/UnifiedBookingPage.jsx`** — `BOOKING_LABELS` config for the public booking page
- **`client/src/HostLanding.jsx`** — hero/features/pricing copy adapted for all types
- **`client/src/BusinessDashboard.jsx`** — `getBusinessTypeConfig()` covers 15+ types
- **`server/src/services/emailService.js`** — `BOOKING_EMAIL_LABELS` config for email templates

Each map has an entry for every supported business type + a `_default` fallback for unknown types. Adding a new business type = adding an entry to each map. No if/else chains, no order bugs, no branches that silently fall through.

**Full business types supported:**
- **Stays:** `hotel`, `apartment`, `event_hall`, `event`
- **Food:** `restaurant`, `diner`, `cafe`, `other_food`
- **Others:** `sports`, `spa`, `beauty_salon`, `salon` (legacy), `nails`, `car_wash` / `car-wash` / `carwash`, `activity_place`

---

## 🛠️ Complete Tech Stack

### Frontend

| Technology | Version | Purpose |
|------------|---------|---------|
| React | 19.0.0 | UI framework |
| Vite | 5.4.21 | Build tool & dev server |
| `React.createElement` | — | **No JSX** — all components created programmatically |
| Lucide React | Latest | Icon library |
| React Hot Toast | Latest | Toast notifications |
| React Router DOM | Latest | Routing |
| Fetch API | Native | HTTP requests to backend |

### Backend

| Technology | Version | Purpose |
|------------|---------|---------|
| Node.js | 20.18.0 | Runtime environment |
| Express | 5.0.0 | Web framework |
| JWT | Latest | Authentication tokens |
| bcryptjs | Latest | Password hashing |
| CORS | Latest | Cross-origin resource sharing |
| express-rate-limit | Latest | Rate limiting (100 req/15min) |
| Node DNS | Native | Custom domain verification |
| dotenv | Latest | Environment variables |
| axios | Latest | HTTP client for Resend API |

### Database & Storage

| Technology | Purpose |
|------------|---------|
| Supabase (PostgreSQL) | Main database — users, businesses, bookings, rooms, gallery |
| Supabase Storage | Image storage — logos, item images (buckets: `venue-images`, `business-images`) |

### External Services

| Service | Purpose | Status |
|---------|---------|--------|
| Paystack | Payment processing | ✅ Test mode |
| Resend | Email delivery | ✅ **Verified + working** |
| Render | Hosting (frontend + backend) | ✅ Live |
| Namecheap | Domain registrar (`myplazzaa.com`) | ✅ Active |
| Google Fonts | Plus Jakarta Sans (fallback for Mail Sans Roman) | ✅ Live |

---

## 📁 Database Schema

### Tables

```sql
-- Users
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  full_name VARCHAR(255),
  role VARCHAR(50) DEFAULT 'owner',
  business_id UUID REFERENCES businesses(id),
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Businesses
CREATE TABLE businesses (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  name VARCHAR(255) NOT NULL,
  slug VARCHAR(255) UNIQUE NOT NULL,
  business_type VARCHAR(100),
  -- Full list of values seen in production:
  --   hotel, apartment, event_hall, event
  --   restaurant, diner, cafe, other_food
  --   sports, spa, beauty_salon, salon, nails, car_wash, car-wash, activity_place
  description TEXT,
  email VARCHAR(255),
  phone VARCHAR(50),
  address TEXT,
  state VARCHAR(100),
  city VARCHAR(100),
  logo_url TEXT,           -- ← primary image source (BusinessProfile writes here)
  cover_image TEXT,        -- ← legacy field, still respected if set
  about_text TEXT,
  story_image TEXT,
  status VARCHAR(50) DEFAULT 'pending',  -- pending | approved | rejected
  is_active BOOLEAN DEFAULT TRUE,        -- ← homepage filter uses this 🆕
  booking_limit INTEGER DEFAULT 50,
  current_booking_count INTEGER DEFAULT 0,
  subscription_status VARCHAR(50) DEFAULT 'free',  -- free | starter | pro
  ref_id VARCHAR(50) UNIQUE,
  property_type VARCHAR(100),
  property_size VARCHAR(50),
  bedrooms INTEGER DEFAULT 0,
  bathrooms INTEGER DEFAULT 0,
  parking_spaces INTEGER DEFAULT 0,
  year_built INTEGER,
  furnishing_status VARCHAR(50) DEFAULT 'unfurnished',
  listing_status VARCHAR(50) DEFAULT 'for_rent',
  features JSONB DEFAULT '[]'::jsonb,
  amenities JSONB DEFAULT '[]'::jsonb,
  area_guide JSONB DEFAULT '[]'::jsonb,
  view_count INTEGER DEFAULT 0,
  phone_numbers JSONB DEFAULT '[]'::jsonb,
  custom_domain VARCHAR(255),
  is_domain_verified BOOLEAN DEFAULT FALSE,
  domain_verification_code VARCHAR(255),
  domain_verification_expires TIMESTAMP,
  domain_verified_at TIMESTAMP,
  subscribed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Rooms (serves as generic "Menu & Services" items)
CREATE TABLE rooms (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  business_id UUID REFERENCES businesses(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  price_per_night DECIMAL(10,2) NOT NULL,
  base_price DECIMAL(10,2),
  capacity INTEGER,
  amenities TEXT[],
  images JSONB DEFAULT '[]'::jsonb,  -- ← first item's images[0] is homepage fallback 🆕
  status VARCHAR(50) DEFAULT 'available',
  type VARCHAR(50),
  address TEXT,
  venue_description TEXT,
  included_guests INTEGER DEFAULT 0,
  max_capacity INTEGER DEFAULT 0,
  extra_guest_price DECIMAL(10,2) DEFAULT 0,
  sort_order INTEGER DEFAULT 0,
  property_type VARCHAR(100),
  bathrooms INTEGER DEFAULT 0,
  parking_spaces INTEGER DEFAULT 0,
  year_built INTEGER,
  furnishing_status VARCHAR(50) DEFAULT 'unfurnished',
  listing_status VARCHAR(50) DEFAULT 'for_rent',
  features JSONB DEFAULT '[]'::jsonb,
  area_guide JSONB DEFAULT '[]'::jsonb,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Bookings
CREATE TABLE bookings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  business_id UUID REFERENCES businesses(id) ON DELETE CASCADE,
  room_id UUID REFERENCES rooms(id) ON DELETE CASCADE,
  booking_reference VARCHAR(255) UNIQUE NOT NULL,
  customer_name VARCHAR(255) NOT NULL,
  customer_email VARCHAR(255) NOT NULL,
  customer_phone VARCHAR(50),
  check_in_date DATE NOT NULL,
  check_out_date DATE NOT NULL,
  number_of_guests INTEGER DEFAULT 1,
  total_amount DECIMAL(10,2) NOT NULL,
  payment_method VARCHAR(50),
  payment_status VARCHAR(50) DEFAULT 'pending',
  payment_reference VARCHAR(255),
  amount_paid DECIMAL(10,2),
  status VARCHAR(50) DEFAULT 'pending',
  special_requests TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Staff (kept for future use — not currently accessible)
CREATE TABLE staff ( ... );

-- Other tables: admin_users, admin_sessions, business_sessions,
-- business_gallery, subscription_upgrades, availability, operating_hours
```

### Design notes on schema

- **`rooms` table is a misnomer** — it serves as the generic "items" table for every business type.
- **`check_in_date` / `check_out_date`** are generic date fields — "the date of the booking" for every business type.
- **`business_type`** drives the entire adaptive label system. Values must match exactly between `BusinessSignup.jsx`, `HomePage.jsx`, `UnifiedBookingPage.jsx`, `HostLanding.jsx`, `BusinessDashboard.jsx`, and `emailService.js`.
- **Image fields explained:**
  - `logo_url` — set by BusinessProfile, the current primary source
  - `cover_image` — legacy, no longer written by any UI, but still selected and respected
  - `rooms[].images[]` — per-item; the first is labelled "Primary" in RoomPage and is the homepage fallback

---

## 🎨 Complete Design System

### Brand Colors

**Plazzaa brand palette (from the official brand kit):**

| Name | Hex | Usage |
|------|-----|-------|
| **Primary Blue** | `#2563EB` | Logo, brand accents |
| **Primary Yellow** | `#FFD166` | Future accents |
| **Neutral Grey** | `#8E8E93` | Future muted text |
| **Dark Charcoal** | `#1B1E28` | Future headings |
| **Tertiary Blue Tint** | `#E8F4FF` | Future backgrounds |
| **Tertiary Cream** | `#FFF9EC` | Future warm accents |

**Current app theme (indigo — pending post-demo palette swap):**

| Color Name | Hex Value | Usage |
|------------|-----------|-------|
| Primary Indigo | `#4F46E5` | Buttons, active states |
| Indigo Light | `#6366F1` | Hover, borders |
| Indigo Dark | `#4338CA` | Active/pressed |
| Indigo Background | `#EEF2FF` | Highlights, section bg |
| White | `#FFFFFF` | Main background |
| Text Primary | `#1A1F36` | Headings |
| Text Secondary | `#475569` | Body |
| Text Muted | `#64748B` | Hints |
| Success Green | `#10B981` | Approved, delivered |
| Warning Yellow | `#F59E0B` | Pending |
| Danger Red | `#EF4444` | Errors, delete |

**⚠️ Pending:** Full theme colour swap from indigo `#4F46E5` to Plazzaa Blue `#2563EB`. Scheduled for after the Wednesday/Friday deploy.

### Typography

- **Primary font (brand):** Mail Sans Roman (Bold / Medium / Regular)
- **Current web font:** Plus Jakarta Sans (fallback — visually similar)
- **Font stack in `index.html`:** `'Mail Sans Roman', 'Plus Jakarta Sans', system-ui, ...`
- **To switch to Mail Sans:** Emmanuel needs to provide `.woff2` files → save to `client/public/fonts/` → add `@font-face` rules in `index.html`. The stack already falls back correctly.

### Responsive Breakpoints

- **Mobile-first:** designed at 375px first
- **Tablet:** 768px (`isDesktop` check)
- **Desktop:** 1024px+

**Golden rule:** Design for worst-case (375px) first, then expand. Desktop is "mobile + more space", not "desktop minus something broke".

---

## 🧩 Complete Component Library

### Frontend Components

| Component | File | Purpose |
|-----------|------|---------|
| Homepage | `HomePage.jsx` | **Stays/Food/Others cards, group search, hero per group, real "Available on Plazzaa" grid 🆕** |
| Admin Login | `AdminLogin.jsx` | Writes to `admin_token` |
| Admin Dashboard | `AdminDashboard.jsx` | Reads `admin_token`, scoped logout |
| Business Login | `BusinessLogin.jsx` | Writes to `business_token` |
| Business Dashboard | `BusinessDashboard.jsx` | Reads `business_token`, full 15+ type config, scoped logout |
| Business Settings | `BusinessSettings.jsx` | Hours, blocked dates, custom domain, phones |
| Unified Booking Page | `UnifiedBookingPage.jsx` | Business-type-aware public booking page + receipt |
| Owner Properties Page | `OwnerPropertiesPage.jsx` | Business info, stats, filter tabs |
| Menu & Services | `RoomPage.jsx` | 6-tab editor (Basic, Details, Images, Features, Amenities, Area Guide) |
| Bookings Manager | `BookingsManager.jsx` | View/update bookings |
| Staff Management | `StaffManagement.jsx` | Kept for future use — not accessible |
| Business Profile | `BusinessProfile.jsx` | Full edit, logo upload only |
| UpgradePage | — | Full page upgrade flow |
| Host Landing Page | `HostLanding.jsx` | Stays/Food/Others marketing, 13-image slideshow, Plazzaa wordmark |
| Business Signup | `BusinessSignup.jsx` | 2-step flow, grouped dropdown |
| API wrapper | `api.js` | Scoped token manager (`admin` vs `business`) |
| ~~DestinationCards~~ | ~~`components/DestinationCards.jsx`~~ | **REMOVED from HomePage render — hardcoded fake data 🆕** |
| ~~PopularStays~~ | ~~`components/PopularStays.jsx`~~ | **REMOVED from HomePage render — hardcoded fake data 🆕** |

### Static Assets (`client/public/`)

| File | Purpose |
|------|---------|
| `plazzaa-logo-blue.png` | Blue wordmark — used in headers |
| `plazzaa-logo-white.png` | White wordmark — used in dark footers |
| `plazzaa-icon-1.png` | Icon-only blue P (spare) |
| `plazzaa-icon-2.png` | Icon-only white P on blue (spare) |
| `plazzaa-favicon.png` | Browser tab icon |

### Backend Routes (Complete API)

**Authentication**
```
POST /api/admin/login
POST /api/businesses/login
POST /api/businesses/register
POST /api/staff/login (kept for future)
```

**Admin**
```
GET    /api/admin/businesses
PUT    /api/admin/businesses/:id/status
DELETE /api/admin/businesses/:id
GET    /api/admin/stats
GET    /api/admin/pending-upgrades
POST   /api/admin/subscription/verify
```

**Public business**
```
GET    /api/businesses                    ← now joins rooms + filters is_active=true 🆕
GET    /api/businesses/featured
GET    /api/businesses/search/category    ← supports comma-separated groups
GET    /api/businesses/slug/:slug
GET    /api/domain-info
GET    /api/businesses/:businessId/rooms
GET    /api/businesses/:businessId/gallery
GET    /api/businesses/:businessId/booking-capacity
```

**Authenticated business**
```
GET    /api/businesses/profile
PUT    /api/businesses/:id
GET    /api/businesses/:id/bookings
GET    /api/businesses/:id/subscription
POST   /api/businesses/:id/upgrade-request
```

**Enhanced business routes**
```
GET    /api/businesses/:identifier/enhanced
GET    /api/businesses/:businessId/gallery/public
GET    /api/businesses/:businessId/phone-numbers/public
PUT    /api/businesses/:businessId/features
PUT    /api/businesses/:businessId/amenities
PUT    /api/businesses/:businessId/area-guide
PUT    /api/businesses/:businessId/phone-numbers
PUT    /api/businesses/:businessId/property-details
POST   /api/businesses/:businessId/gallery
DELETE /api/businesses/:businessId/gallery/:imageId
```

**Items (rooms)**
```
POST   /api/businesses/:businessId/rooms/create
PUT    /api/businesses/:businessId/rooms/:roomId
DELETE /api/businesses/:businessId/rooms/:roomId
POST   /api/businesses/:businessId/rooms/:roomId/upload-image
DELETE /api/businesses/:businessId/rooms/:roomId/images
```

**Bookings**
```
POST   /api/bookings
GET    /api/bookings/reference/:reference
POST   /api/bookings/:reference/cancel
PATCH  /api/bookings/:id
```

**Payments**
```
POST   /api/create-payment
POST   /api/verify-payment
```

**Domain & verification**
```
POST   /api/businesses/generate-verification
POST   /api/businesses/check-verification
GET    /api/domain-info
```

---

## 🚀 Complete Feature Implementation

### 1. Business Registration (2-step)
- **Step 1:** Account (email, password, name, business name + type, phone)
- **Step 2:** Location (address, city, state)
- **Grouped business type dropdown:** Stays / Food / Others
- Ref ID generation `BH-XXXXXXXX`

### 2. Authentication
- Scoped JWT tokens in localStorage
  - `admin_token` for admin routes
  - `business_token` for business routes
  - Legacy `auth_token`/`token` fallback for migration
- bcryptjs (10 rounds) password hashing
- Role-based access (admin, owner)
- Admin sessions (1 day), Business sessions (7 days)
- Sessions no longer clobber each other

### 3. Admin Dashboard
- Stats overview, business management, approve/reject, delete
- Subscription verification
- Welcome + approval emails fire from here
- Reads `admin_token` first; only clears admin scope on 401

### 4. Business Dashboard
- Stats cards (items, bookings, revenue, usage)
- Copy Booking Link card
- Left-aligned sidebar navigation
- Menu & Services, Bookings, Profile, Settings, Plans tabs
- Full 15+ business type config — restaurant, diner, cafe, other_food, hotel, apartment, event_hall, event, sports, spa, beauty_salon, salon, nails, car_wash, activity_place
- Reads `business_token` first; scoped logout

### 5. Public Booking Page (UnifiedBookingPage)
- Business-type-aware labels
- Gallery grid, venue info header, features, amenities, area guide
- "Marketed by" card
- Lightbox gallery, floating action buttons, call modal
- Book Now modal — single date, no guest count
- Professional booking receipt
- No back navigation

### 6. Owner Properties Page
- Business info, stats, areas covered, filter tabs, listing cards

### 7. Menu & Services (RoomPage)
- Renamed from "Rooms"
- 6 tabs: Basic, Details, Images, Features, Amenities, Area Guide
- Professional delete confirmation modal
- Image upload (drag-drop + button, 16 limit, reorder, Primary badge)

### 8. Business Profile
- All fields editable, logo upload only (cover image + gallery removed)

### 9. Email Notifications ✅ WORKING
- Domain verified: `myplazzaa.com`
- Sender: `bookings@myplazzaa.com` (Plazzaa <...>)
- Business-type-aware templates — `BOOKING_EMAIL_LABELS` config
- Delivered end-to-end with real message IDs from Resend
- Silent failures eliminated via `logEmailResult()`
- Env var sanitization via `cleanEnv()`
- Functions: `sendEmail`, `sendBookingConfirmation`, `sendWelcomeEmail`, `sendApprovalEmail`, `sendReminderEmail`

### 10. Subscription System
- Free (50 bookings), Starter (₦30k, 100 bookings), Pro (₦50k, unlimited)
- Upgrade request flow with bank transfer instructions
- Admin verification activates upgrade

### 11. Admin Login & Sessions
- `/admin` route, `admin_sessions` table, 1-day expiry
- Scoped token storage

### 12. Domain & Deployment ✅ LIVE
- Production: `https://myplazzaa.com`
- Backend: `https://booking-backend-clean.onrender.com`
- Frontend: `https://booking-frontend-clean.onrender.com` (legacy)
- DNS: Namecheap → Render
- Business-level custom domains supported

### 13. Database-Driven Content
- Features, amenities, area guide, phones, property details, images — all per-item

### 14. Host Landing Page ✅ UPDATED 19–20 SEPT
- Stays/Food/Others positioning
- 13-image slideshow (7 event + 6 new across categories)
- Features: "Instant Notifications" replaced "Staff Management"
- Footer email: `hello@myplazzaa.com`
- Hero H1: "The all-in-one platform for **businesses** in Nigeria"
- Plazzaa blue wordmark PNG in header
- Plazzaa white wordmark PNG in footer
- Responsive: hero 56px→32px, sections 80px→50px padding on mobile

### 15. Business Signup
- 2-step flow with grouped business type dropdown

### 16. Homepage Category Cards ✅ UPDATED 19–20 SEPT
- Stays / Food / Others
- Subtitles: "Hotels, apartments & venues" / "Restaurants, cafés & more" / "Spas, salons & activities"
- Icons: `Building2` / `Utensils` / `Sparkles`
- Hero copy + image per group
- Group search — comma-separated business types
- "Book Now" routes to `/book/{slug}`
- Plazzaa blue wordmark PNG in header
- Responsive: hero 420px→260px, gaps scale down

### 17. Plazzaa Brand Rebrand ✅ LIVE 20 SEPT
- Logo swap: `plazzaa-logo-blue.png` (header), `plazzaa-logo-white.png` (footer)
- Favicon: `plazzaa-favicon.png` (white P on blue)
- Browser tab: "Plazzaa — Bookings For Businesses"
- Footer copyright: "© 2026 Plazzaa. All rights reserved."
- All "Booking Hub" strings → "Plazzaa" across visible UI
- Font stack updated: Mail Sans Roman prepended, falls back to Plus Jakarta Sans

### 18. Responsive Polish ✅ 20 SEPT
- All logos scale: 28px desktop → 22px mobile
- All grids: multi-column desktop → single column mobile
- All paddings/margins: scale via `isDesktop` ternaries
- Back button: "Back to Home" → "Back" on mobile
- Tested at 375 / 768 / 1280px

### 19. Business Type Label Fix ✅ 21 SEPT
- Problem: Business dashboard showed "Business" instead of the actual type for restaurant, spa, salon, nails, car-wash, café, apartment, etc.
- Fix: New `getBusinessTypeConfig()` in `BusinessDashboard.jsx` covers 15+ types with proper labels, icons, colours, and item nouns.
- Case-insensitive lookup so `"Restaurant"` and `"restaurant"` both work.
- Handles both `car_wash` and `car-wash` variants.

### 20. Scoped Auth Tokens ✅ 21 SEPT
- Problem: Logging into a business overwrote the admin session; business logout wiped both.
- Fix: Scoped localStorage keys — `admin_token` and `business_token`. Each dashboard reads and clears only its own scope. `api.js` picks the right token based on route.

### 21. Homepage Real-Businesses Fix ✅ 22 SEPT 🆕
- **Problem (spotted by UX designer testing with Olusegun):** Homepage showed fake businesses — hardcoded location cards ("Victoria Island", "Lekki", "Abuja City Center", "Wuse District" with fabricated hotel counts) and hardcoded "Popular Stays" (Eko Hotel, Transcorp Hilton, The Guest House Lagos with fabricated ratings and prices). Search result cards also used hardcoded stock images keyed by business_type and displayed hardcoded `₦0`, hardcoded `4.9` rating, and a fake fallback description. Clicking some cards triggered a "Please select a location" toast because `PopularStays.onSelectHotel` set `location` to a display string that didn't match any `city` value in the DB.
- **Fix:**
  - **Backend** (`server.js`): `GET /api/businesses` now selects `*, rooms(id, images, name)` and filters `.eq('is_active', true)` alongside the existing `.eq('status', 'approved')`.
  - **Frontend** (`HomePage.jsx`): `DestinationCards` and `PopularStays` imports and render blocks removed. New "Available on Plazzaa" grid fed by `/api/businesses`. `resolveBusinessImage()` picks `cover_image` → `logo_url` → `rooms[0].images[0]` → placeholder. `formatBusinessArea()` shows real city/state or nothing — never a fake "Lagos". No fake price, no fake rating, no fake description.
  - Clicking a card → `navigate('/book/{slug}')` directly. No location check, no toast.
- **Status:** Code written. **Local testing pending.**

---

## 🐛 Complete Bug Fix History

| # | Issue | Solution | Status |
|---|-------|----------|--------|
| 1 | Git Repository Corruption | Fresh clone from GitHub | ✅ FIXED |
| 2 | Admin Login Demo Credentials | Removed demo credentials | ✅ FIXED |
| 3 | Password Toggle Not Working | Added state + toggle handlers | ✅ FIXED |
| 4 | AdminLogin ReferenceError | Added React import | ✅ FIXED |
| 5 | AdminDashboard Build Failures | Fixed syntax | ✅ FIXED |
| 6 | Render Deployment Failing | Fixed syntax errors | ✅ FIXED |
| 7 | Home Button Missing | Added Home button | ✅ FIXED |
| 8 | Mobile Responsiveness | Mobile-specific styles | ✅ FIXED |
| 9 | Email Notifications Blocked | Domain verified + sender switched + sanitized env vars | ✅ FIXED 19 Sept |
| 10–41 | *(see previous versions — all resolved)* | — | ✅ FIXED |
| 42 | Business-Type-Aware Forms | Config-driven `RoomPage` per business type | ✅ FIXED 14 Sept |
| 43 | Business-Type-Aware Booking Page | `BOOKING_LABELS` config in `UnifiedBookingPage` | ✅ FIXED 18 Sept |
| 44 | Restaurant/Spa/Sports Showing "Check-in/Check-out" | Fixed broken `if/else` picker, config-driven labels | ✅ FIXED 18 Sept |
| 45 | CORS Blocking `myplazzaa.com` | Added to `ALLOWED_ORIGINS` env var | ✅ FIXED 18 Sept |
| 46 | Resend Testing Limitation (403) | Verified domain, switched `FROM_EMAIL` | ✅ FIXED 19 Sept |
| 47 | Malformed `from` Header (422) | Sanitized env vars via `cleanEnv()` | ✅ FIXED 19 Sept |
| 48 | Silent Email Failures | `.then()` checks + `logEmailResult()` helper | ✅ FIXED 19 Sept |
| 49 | Git Merge Conflict in `emailService.js` | Resolved — kept smart email version | ✅ FIXED 18 Sept |
| 50 | Homepage Still on Hotels/Sports/Events | Stays/Food/Others cards + group search + Book Now fix | ✅ FIXED 19 Sept |
| 51 | Search didn't support groups | Backend `.in()` for comma-separated types | ✅ FIXED 19 Sept |
| 52 | "Book Now" broke on restaurant/spa | Route to `/book/{slug}` (UnifiedBookingPage) | ✅ FIXED 19 Sept |
| 53 | HostLanding was event-venue-only | Repositioned for Stays/Food/Others + 6 new slides | ✅ FIXED 19 Sept |
| 54 | Logo was "BookingHub" text | Swap to Plazzaa blue wordmark PNG | ✅ FIXED 20 Sept |
| 55 | Footer still said "Booking Hub" | Rebranded to Plazzaa white wordmark | ✅ FIXED 20 Sept |
| 56 | Browser tab showed emoji/placeholder | Favicon set to `plazzaa-favicon.png` + apple-touch-icon | ✅ FIXED 20 Sept |
| 57 | Tab title was "Book Anything in Nigeria" | Changed to "Plazzaa — Bookings For Businesses" | ✅ FIXED 20 Sept |
| 58 | Mask-image trick rendered filled purple square | Replaced with plain `<img>` of blue wordmark PNG | ✅ FIXED 20 Sept |
| 59 | Logo too big on some screens | Resized 40px→28px desktop, 32px→22px mobile | ✅ FIXED 20 Sept |
| 60 | Business dashboard showed "Business" for restaurant/spa/etc. | Full `getBusinessTypeConfig()` covers 15+ types | ✅ FIXED 21 Sept |
| 61 | Admin session killed on business logout | Scoped tokens: `admin_token` vs `business_token` | ✅ FIXED 21 Sept |
| 62 | **Homepage showed fake businesses + stock images + fake ₦0/4.9 ratings + "select location" toast** | **Removed DestinationCards/PopularStays; new "Available on Plazzaa" grid; backend joins rooms + filters is_active** | **🟡 FIXED IN CODE — local test pending 22 Sept** 🆕 |

---

## 💡 Breakthroughs & Key Decisions

1. Indigo Branding (#4F46E5) — will swap to Plazzaa Blue post-deploy
2. Mobile-First Philosophy (375px first)
3. No JSX (`React.createElement` throughout)
4. Supabase for DB + Storage
5. Render for Deployment
6. Unified Booking Page
7. Plazzaa Rebrand — complete through all visible UI
8. Subscription System (Free/Starter/Pro)
9. Database-Driven Content
10. Multi-Venue Architecture
11. Venue Image Upload with auto-bucket creation
12. Category-Based Features & Amenities
13. Tabbed Room Management (6 tabs)
14. Booking Receipt
15. Copy Booking Link Card
16. Business Categories Broadened (Stays / Food / Others)
17. "Menu & Services" Rename
18. Guest Count Relocation — moved to confirmation step
19. Business-Type-Aware System — config-driven labels across homepage, booking page, emails, dashboard
20. Industry-Standard Approach — one booking model, adaptive presentation
21. Custom Domain Support — `myplazzaa.com` live
22. Resend Domain Verification — `bookings@myplazzaa.com`
23. Email Observability — silent failures eliminated
24. Defensive Configuration — env vars sanitized at load time
25. Group Search — homepage search translates groups to specific types
26. HostLanding Repositioning — from event-venue-only to all-business-types
27. Plazzaa Logo Live — blue wordmark PNG used directly (no CSS tricks)
28. Font Fallback Strategy — Mail Sans prepended, Plus Jakarta fallback
29. Responsive-First Rebrand — tested at 375 / 768 / 1280px
30. Full Business-Type Config in Dashboard — 15+ types, case-insensitive
31. Scoped Auth Tokens — admin and business sessions independent
32. **🆕 Real-Data-Only Homepage** — no hardcoded businesses, images, prices, or ratings on the public grid. The homepage is now a mirror of admin-approved, active businesses.
33. **🆕 Backend-Join Pattern for Homepage Cards** — `select('*, rooms(id, images, name)')` avoids N+1 fetches and gives the homepage the business owner's own uploaded image without a second HTTP round-trip.

---

## 📊 Current Project Status (22 SEPTEMBER 2026)

### Deployment Status

| Platform | Service | URL | Status |
|----------|---------|-----|--------|
| Custom domain | Frontend | `https://myplazzaa.com` | ✅ LIVE |
| Render | Frontend | `https://booking-frontend-clean.onrender.com` | ✅ LIVE (legacy) |
| Render | Backend | `https://booking-backend-clean.onrender.com` | ✅ LIVE |
| Render | Health Check | `/health` endpoint | ✅ Working |
| GitHub | Source Code | `https://github.com/olusegun-sys/booking-hub-clean` | ✅ Updated |
| Resend | Email domain | `myplazzaa.com` | ✅ Verified |
| Namecheap | Domain registrar | `myplazzaa.com` | ✅ Active |

### Test Credentials

| Role | Email | Password |
|------|-------|----------|
| Admin | `admin@bookinghub.com` | `admin123` |
| Business Owner | `test@hotel.com` | (any password) |
| Sports test business | `peaksports@gmail.com` | — |

### Feature Completion Matrix

| Feature | Status | Completion |
|---------|--------|------------|
| User Authentication | ✅ | 100% |
| Scoped Auth Tokens (admin vs business) | ✅ | 100% |
| Business Registration (2-step) | ✅ | 100% |
| Admin Dashboard | ✅ | 100% |
| Business Dashboard | ✅ | 100% |
| Full 15+ Business Type Config | ✅ | 100% |
| Copy Booking Link Card | ✅ | 100% |
| Menu & Services (RoomPage) | ✅ | 100% |
| Per-Item Features/Amenities/Area Guide/Details/Images | ✅ | 100% |
| Custom Domain (per-business) | ✅ | 100% |
| Public Booking Page | ✅ | 100% |
| Business-Type-Aware Booking Page | ✅ | 100% |
| Host Landing Page | ✅ | 100% |
| Homepage Stays/Food/Others cards | ✅ | 100% |
| Group search | ✅ | 100% |
| "Book Now" routes to `/book/{slug}` | ✅ | 100% |
| Plazzaa logo (blue wordmark) | ✅ | 100% |
| Plazzaa logo (white wordmark footer) | ✅ | 100% |
| Plazzaa favicon | ✅ | 100% |
| Tab title: "Plazzaa — Bookings For Businesses" | ✅ | 100% |
| Responsive across all devices | ✅ | 100% |
| Font stack ready for Mail Sans Roman | ✅ | 100% |
| Paystack Integration | ✅ | 90% (test mode) |
| Email Notifications | ✅ | 100% |
| Business-Type-Aware Emails | ✅ | 100% |
| Resend Domain Verification | ✅ | 100% |
| Custom Domain (`myplazzaa.com`) | ✅ | 100% |
| Subscription System | ✅ | 100% |
| Staff Feature Removed | ✅ | 100% |
| Plazzaa Rebrand (full UI) | ✅ | 100% |
| **Homepage real-businesses only 🆕** | 🟡 | **95% — code done, local test pending** |
| Theme colour match (indigo → Plazzaa Blue) | ⏳ | Pending — post-deploy |
| Mail Sans Roman `.woff2` files | ⏳ | Pending — Emmanuel |
| Landing Page Redesign | ⏳ | Pending (Emmanuel) |
| Guest Count Relocation | ⏳ | Pending (Jeremiah) |

---

## 🎯 What's Left to Do

### 🔴 Immediate — 22 Sept (Tonight)

| Task | Owner | Priority | Status |
|------|-------|----------|--------|
| Paste new `server.js` (with `/api/businesses` join + is_active filter) | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Paste new `HomePage.jsx` (remove DestinationCards/PopularStays, add real grid) | Olusegun | 🔴 HIGH | ⏳ PENDING |
| **Back up `server.js` before editing** (`cp server.js server.js.backup.22sept`) | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Start backend, verify no errors | Olusegun | 🔴 HIGH | ⏳ PENDING |
| `curl http://localhost:5000/api/businesses` — verify `rooms` array is present | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Start frontend, open localhost:5173 in incognito | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Verify: fake cards gone, "Available on Plazzaa" grid appears, real images, no toast on click | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Test at 375px and 1280px | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Commit + push to GitHub | Olusegun | 🔴 HIGH | ⏳ PENDING |
| Confirm Render redeploys clean | Olusegun | 🔴 HIGH | ⏳ PENDING |

### 🟡 This Week (Post-Fix)

| Task | Priority | Notes |
|------|----------|-------|
| Merge Emmanuel's UI changes into `main` | 🔴 HIGH | Group decision, end of week |
| Deploy B2B platform changes to `myplazzaa.com` | 🔴 HIGH | Wednesday 24 Sept target; Friday 26 Sept worst-case |
| Terrence to review platform after deploy | 🟡 MEDIUM | — |
| Rotate exposed Resend API key | 🔴 HIGH | Pasted in chat history |
| Rotate exposed Supabase service role key | 🔴 HIGH | Pasted in chat history |
| Rotate exposed Paystack secret key | 🔴 HIGH | Pasted in chat history |
| Change admin password (`admin@bookinghub.com / admin123`) | 🔴 HIGH | Publicly known |
| Full theme colour swap (indigo → Plazzaa Blue `#2563EB`) | 🟡 MEDIUM | — |
| Set up `hello@myplazzaa.com` mailbox | 🟡 MEDIUM | Footer advertises this |
| Pilot Business Partnerships (2–3 businesses, 2–3 month trial) | 🟡 MEDIUM | Group decision |
| Recruit New Businesses (target those without booking platforms) | 🟡 MEDIUM | Group decision |
| Determine Pricing Strategy | 🟡 MEDIUM | Group decision |
| Brainstorm Monetisation (alternative revenue, credit-limit UX) | 🟡 MEDIUM | Group decision |

### 🟢 Phase 2 (After Pilot Feedback)

- Payments → Paystack production mode
- Add Mail Sans Roman `.woff2` files
- WhatsApp integration
- Automated booking reminders
- Analytics dashboard with charts
- Reviews and ratings
- Google Maps re-integration
- Staff feature re-enable
- Plazzaa vetting standards
- Time slots for restaurants/spas (if data shows need)
- Multi-item cart for restaurants (if data shows need)
- Recurring bookings for sports (if data shows need)
- Replace Google-hosted images in HostLanding slideshow
- Consider replacing homepage hero images with real business imagery

### Known Issues

| Issue | Priority | Status |
|-------|----------|--------|
| Homepage fix not yet tested locally 🆕 | 🔴 High | Tonight |
| 4 exposed secrets in chat history need rotation | 🔴 High | Post-deploy |
| `hello@myplazzaa.com` may not have a mailbox set up | 🟡 Medium | Post-deploy |
| No test environment — updates deploy directly live | 🟡 Medium | Noted by Yannick |
| No real businesses onboarded yet | 🟡 Medium | After deploy |
| Paystack not fully tested in production | 🟡 Medium | Test mode only |
| Theme still indigo — Plazzaa Blue swap pending | 🟢 Low | Post-deploy |
| Mail Sans Roman font files not yet added | 🟢 Low | Emmanuel |
| Supabase nested `rooms` query relies on FK cache — if it errors, swap to two-step query 🆕 | 🟡 Medium | During local test |

---

## 📖 For the Next Chat: Pick Up Here

### Current State Summary

✅ **The app is FULLY DEPLOYED at `https://myplazzaa.com`**
- Admin: `https://myplazzaa.com/admin`
- Business Login: Click "Business Login" on homepage
- Sample booking page: `https://myplazzaa.com/book/{slug}`
- Business marketing page: `https://myplazzaa.com/become-host`
- Backend: `https://booking-backend-clean.onrender.com`

✅ **All core features are complete and working**
- Full Plazzaa brand identity live
- Business-type-aware homepage, booking page, emails, dashboard
- Group search working
- Responsive across all devices
- Emails working end-to-end
- Scoped auth tokens (admin and business sessions independent)

🟡 **Newest work in-flight (22 Sept):**
- Homepage real-businesses fix — code written, **NOT YET TESTED LOCALLY, NOT YET PUSHED**
- Files changed: `server.js` (one route), `client/src/HomePage.jsx` (full rewrite)
- Files removed from render (but kept on disk): `DestinationCards.jsx`, `PopularStays.jsx`

### 🚨 Immediate Next Steps

1. **Back up `server.js`:** `cp server.js server.js.backup.22sept`
2. **Paste the new `server.js`** — the one with `select('*, rooms(id, images, name)')` and `.eq('is_active', true)`
3. **Start backend:** `node server.js` — no errors
4. **Test endpoint:** `curl http://localhost:5000/api/businesses` — verify each business has a `rooms` array
   - **If it errors about missing relation:** swap the nested select for a two-step query (fetch businesses, then fetch all rooms in one `.in('business_id', ids)` call)
5. **Paste the new `HomePage.jsx`** — the one without `DestinationCards`/`PopularStays` imports and with the "Available on Plazzaa" grid
6. **Start frontend:** `npm run dev`
7. **Open `http://localhost:5173` in incognito**
8. **Verify:**
   - "Victoria Island", "Lekki", "Abuja City Center", "Wuse District" cards → **gone**
   - "Eko Hotel", "Transcorp Hilton", "The Guest House Lagos" → **gone**
   - New "Available on Plazzaa" grid with real businesses
   - Each card shows the business owner's own uploaded image (logo or primary item image)
   - Clicking a card → straight to `/book/{slug}` — **no toast**
   - Search for a real city → results show real images, no fake ₦0 or 4.9 star
9. **Test at 375px and 1280px**
10. **If all good:** `git add . && git commit -m "homepage: real approved businesses only" && git push`
11. **Verify on `myplazzaa.com`** after Render redeploys

### Key Files (UPDATED)

| File | Purpose |
|------|---------|
| `/client/index.html` | Global CSS, font stack, tab title, favicon |
| `/client/src/App.jsx` | Main app, routing |
| `/client/src/HomePage.jsx` | **Homepage — Stays/Food/Others cards, real "Available on Plazzaa" grid 🆕** |
| `/client/src/AdminDashboard.jsx` | Admin dashboard — reads `admin_token`, scoped logout |
| `/client/src/AdminLogin.jsx` | Admin login — writes `admin_token` |
| `/client/src/BusinessDashboard.jsx` | Business dashboard — full 15+ type config, scoped tokens |
| `/client/src/BusinessLogin.jsx` | Business login — writes `business_token` |
| `/client/src/BusinessSignup.jsx` | 2-step signup, grouped dropdown |
| `/client/src/BusinessSettings.jsx` | Settings |
| `/client/src/BusinessProfile.jsx` | Profile — logo upload only (cover + gallery removed) |
| `/client/src/UnifiedBookingPage.jsx` | Public booking page — business-type-aware labels |
| `/client/src/OwnerPropertiesPage.jsx` | All properties from a business |
| `/client/src/RoomPage.jsx` | Menu & Services management — 6 tabs, image upload with Primary badge |
| `/client/src/HostLanding.jsx` | Business marketing page — 13-image slideshow |
| `/client/src/StaffManagement.jsx` | Kept for future — not accessible |
| `/client/src/api.js` | Scoped token manager |
| `/client/src/components/DestinationCards.jsx` | **Removed from render — kept on disk 🆕** |
| `/client/src/components/PopularStays.jsx` | **Removed from render — kept on disk 🆕** |
| `/server/server.js` | **Express server — `/api/businesses` now joins rooms + filters is_active 🆕** |
| `/server/src/services/emailService.js` | Business-type-aware emails |
| `/server/src/routes/businessRoutes.js` | Enhanced business routes |
| `/server/src/services/paystackService.js` | Paystack integration |
| `/server/src/services/dnsService.js` | DNS verification |
| `/server/src/middleware/domainDetector.js` | Custom domain detection |
| `/server/.env` | Local env vars (gitignored) |
| `/client/public/plazzaa-logo-blue.png` | Blue wordmark asset |
| `/client/public/plazzaa-logo-white.png` | White wordmark asset |
| `/client/public/plazzaa-favicon.png` | Favicon asset |

### The Golden Rule

> **"Design for the worst-case scenario first (375px mobile), then expand for desktop. Desktop is 'mobile + more space' not 'desktop - something broke.'"**

---

## 🏆 Success Criteria

### Completed ✅

- [x] Frontend loads without blank screen
- [x] Backend health check returns 200
- [x] Admin can log in
- [x] Business can log in
- [x] Items can be added/edited/deleted
- [x] Public booking page works
- [x] Gallery images display
- [x] "Pay at venue" creates booking
- [x] Deployed to Render successfully
- [x] Custom domain verification works
- [x] Mobile responsive across all pages
- [x] Subscription system implemented
- [x] Per-item Features, Amenities, Area Guide, Property Details, Images
- [x] Multi-item support
- [x] Owner Properties page
- [x] Booking Receipt after confirmation
- [x] Staff feature removed cleanly
- [x] Copy Booking Link card
- [x] Business category broadening (Stays / Food / Others)
- [x] "Menu & Services" rename
- [x] CTA: "Get Started"
- [x] Business-type-aware booking page
- [x] Business-type-aware emails
- [x] Custom domain `myplazzaa.com` live
- [x] Email notifications working end-to-end
- [x] Resend domain verified
- [x] Silent email failures eliminated
- [x] Homepage group search working
- [x] "Book Now" routes all types to `/book/{slug}`
- [x] HostLanding rebranded for Stays/Food/Others
- [x] 13-image HostLanding slideshow
- [x] Plazzaa logo (blue wordmark) live in headers
- [x] Plazzaa white wordmark live in footer
- [x] Plazzaa favicon live
- [x] Tab title: "Plazzaa — Bookings For Businesses"
- [x] Full responsive polish at 375 / 768 / 1280px
- [x] Font stack ready for Mail Sans Roman
- [x] Full 15+ business type config in BusinessDashboard
- [x] Scoped auth tokens (admin vs business)

### In Flight 🟡

- [ ] **Homepage real-businesses fix — code complete, local test pending** 🆕
- [ ] **Push homepage fix to GitHub after local verification** 🆕
- [ ] **Verify on `myplazzaa.com` after Render redeploys** 🆕

### Pending ⏳

- [ ] Merge Emmanuel's UI changes into `main`
- [ ] Deploy B2B platform changes (Wednesday 24 Sept target)
- [ ] Terrence review after deploy
- [ ] Full theme colour swap (indigo → Plazzaa Blue `#2563EB`)
- [ ] Mail Sans Roman `.woff2` files (Emmanuel)
- [ ] Landing page redesign (Emmanuel)
- [ ] Guest count relocation (Jeremiah)
- [ ] Onboard pilot businesses (2–3 businesses, 2–3 month trial)
- [ ] Determine pricing strategy
- [ ] Brainstorm monetisation
- [ ] Rotate exposed secrets (post-deploy)
- [ ] Set up `hello@myplazzaa.com` mailbox

---

## 🚀 Final Words

### What We've Built

A complete, production-ready booking management platform, live at `myplazzaa.com` with full Plazzaa brand identity. The platform serves any business type — Stays, Food, Others — with a single booking model that adapts labels, emails, category cards, and dashboard presentation per business type. Emails deliver end-to-end. The homepage now shows only real, admin-approved businesses with their own uploaded images. The entire platform is mobile-first, responsive across all devices, deployed on Render, and ready for real businesses.

### What Makes This Special

1. **Mobile-first** — built for Nigerian users
2. **Multi-tenant** — each business gets their own branded page
3. **Business-type-aware** — one booking model, adaptive presentation
4. **Complete** — from registration to booking to payment to email
5. **Professional** — Plazzaa branding, premium UI
6. **Secure** — JWT, bcrypt, rate limiting, DNS verification
7. **Subscription-ready** — Free/Starter/Pro tiers
8. **Database-driven** — all content configurable
9. **Multi-item** — businesses can list multiple items
10. **Per-Item Settings** — each item has its own features, amenities, area guide, images
11. **Copy Booking Link** — prominent on dashboard
12. **Emails working end-to-end** — business-type-aware, delivered, observable
13. **Live on custom domain** — `myplazzaa.com` with SSL
14. **Group search** — Stays/Food/Others cards find the right business types
15. **Everything routes safely** — "Book Now" works for all business types
16. **Fully responsive** — 375px mobile to 1280px+ desktop
17. **Plazzaa brand identity live** — logo, favicon, tab title, footer
18. **Scoped auth tokens** — admin and business sessions independent
19. **Full 15+ business type config** — restaurant, spa, salon, nails, car-wash, café, apartment, etc.
20. **Real-data homepage** — no fake cards, no stock photos, no fabricated prices or ratings 🆕

### The Road Ahead

The code is ready. The deployment is ready. The emails are working. The custom domain is live. The Plazzaa brand identity is live. The dashboard adapts to every business type. The auth tokens are scoped. The homepage now reflects real data. What's left: run the local test tonight, push the homepage fix, confirm Render redeploys clean, merge Emmanuel's UI changes, deploy the combined work by Wednesday, then rotate secrets, swap the theme to Plazzaa Blue, onboard the first pilot businesses, and begin collecting usage data.

---

**"The best time to start was yesterday. The next best time is now."**

Let's ship this homepage fix. 🚀

---

## 📚 Appendix: Quick Reference

### Common Commands

```bash
# Start frontend (development)
cd client && npm run dev

# Start backend (development)
cd server && node server.js

# Build frontend
cd client && npm run build

# Back up server.js before a risky edit
cp server/server.js server/server.js.backup.22sept

# Test the homepage endpoint after editing
curl http://localhost:5000/api/businesses | head -c 2000

# Deploy to Render
git add .
git commit -m "message"
git push origin main
```

### Environment Variables (Required on Render backend)

| Variable | Purpose |
|----------|---------|
| `SUPABASE_URL` | Database connection |
| `SUPABASE_ANON_KEY` | Database auth |
| `SUPABASE_SERVICE_ROLE_KEY` | Database admin |
| `RESEND_API_KEY` | Email sending |
| `FROM_EMAIL` | Sender address (`bookings@myplazzaa.com`) |
| `FROM_NAME` | Sender name (`Plazzaa`) |
| `APP_URL` | App URL (`https://myplazzaa.com`) |
| `PAYSTACK_SECRET_KEY` | Payment processing |
| `PAYSTACK_PUBLIC_KEY` | Payment processing |
| `NODE_ENV` | `production` |
| `ALLOWED_ORIGINS` | CORS (`https://myplazzaa.com,https://www.myplazzaa.com,...`) |

### Key People

- **Developer:** Olusegun (olusegun-sys)
- **Team Lead:** Felicity (Plazzaa)
- **Head of Engineering:** Yannick
- **CEO:** Mr. Terrence
- **UI/UX:** Emmanuel
- **Sales/Outreach:** Adewale Adekunle
- **Engineer:** Jeremiah Olatunde

### Plazzaa Brand Assets (in `client/public/`)

- `plazzaa-logo-blue.png` — blue wordmark (headers)
- `plazzaa-logo-white.png` — white wordmark (dark footers)
- `plazzaa-favicon.png` — browser tab icon
- `plazzaa-icon-1.png` — icon-only variant (spare)
- `plazzaa-icon-2.png` — icon-only variant (spare)

### Design Tokens

- **Primary brand blue:** `#2563EB` (Plazzaa — pending theme swap)
- **Current theme:** Indigo `#4F46E5` (until post-deploy)
- **Font stack:** `'Mail Sans Roman', 'Plus Jakarta Sans', system-ui, ...`
- **Mobile-first breakpoints:** 375 / 768 / 1280px

### Auth Storage Keys

| Key | Purpose |
|-----|---------|
| `admin_token` | Admin JWT (primary) |
| `business_token` | Business JWT (primary) |
| `auth_token` | Legacy fallback (read-only) |
| `token` | Legacy fallback (read-only) |
| `admin` | Admin object (dashboard uses) |
| `currentBusiness` | Current business object |

### Image Resolution Priority (Homepage Cards) 🆕

1. `business.cover_image` — legacy, still respected if set
2. `business.logo_url` — what BusinessProfile writes today
3. `business.rooms[0].images[0]` — first item's primary image from RoomPage
4. `null` → neutral "No image yet" placeholder

---

*Last update: 22 September 2026, evening — homepage fix in flight, local test pending*